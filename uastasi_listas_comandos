# 1. Actualizar dependencias
flutter pub get

# 2. Analizar codigo
flutter analyze

# 3. Regenerar archivos Drift
flutter pub run build_runner build --delete-conflicting-outputs

# 4. Verificar navegacion
bash verify_navigation.sh